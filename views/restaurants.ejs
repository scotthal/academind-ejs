<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include("includes/head") %>
    <link rel="stylesheet" href="styles/restaurants.css" />
  </head>
  <body>
    <%- include("includes/header") %> <%- include("includes/mobile-drawer") %>
    <main>
      <h1>Recommended restaurants</h1>
      <% if (numberOfRestaurants === 0) { %>
      <p>
        No one has suggested any restaurants, yet. Try sharing your favorite!
      </p>
      <% } else { %>
      <p>Find your next favorite restaurants with help of our other users!</p>
      <p>We found <%= numberOfRestaurants %> restaurants.</p>
      <form action="/restaurants" method="GET">
        <input type="hidden" value="<%= order %>" name="order" />
        <button class="btn">Change Order</button>
      </form>
      <ul id="restaurants-list">
        <% for (const restaurant of restaurants) { %>
        <!-- Inserting a comment to make prettier stop being stupid -->
        <%- include("includes/restaurant-item", {restaurant: restaurant}) %>
        <!-- Inserting a comment to make prettier stop being stupid -->
        <% } %>
      </ul>
      <% } %>
    </main>
  </body>
</html>
